#include "monty.h"

/**
 * f_comment - this handles a line that starts with a
 * comment character '#'
 * @head: pointer to the top of the stack
 * @counter: line number of the command
 */

void f_comment(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
}
